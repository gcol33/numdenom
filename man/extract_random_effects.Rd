% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/formula.R
\name{extract_random_effects}
\alias{extract_random_effects}
\title{Extract random effect specifications from formula text}
\usage{
extract_random_effects(rhs_text, data)
}
\arguments{
\item{rhs_text}{Right-hand side of formula as text}

\item{data}{Data frame}
}
\value{
List of random effect specifications, each containing:
\itemize{
\item \code{group_var}: Name of grouping variable
\item \code{group}: Integer vector of group indices (1-based)
\item \code{n_groups}: Number of unique groups
\item \code{has_intercept}: Whether term includes random intercept
\item \code{slope_vars}: Character vector of slope variable names (NULL if none)
\item \code{slope_vars_raw}: Original slope terms before expansion (for reference)
\item \code{slope_vars_clean}: Cleaned slope names for parameter output
\item \code{slope_matrix}: Matrix of slope values (n_obs x n_slopes) if slopes present
\item \code{correlated}: Whether slopes are correlated with intercept (TRUE for \code{|}, FALSE for \code{||})
\item \code{original}: Original formula text
}
}
\description{
Parses random effect terms from formula text, supporting:
\itemize{
\item Random intercepts: \code{(1 | group)}
\item Correlated random slopes: \code{(1 + x | group)} - estimates intercept-slope correlation
\item Uncorrelated random slopes: \code{(1 + x || group)} - no correlation estimated
\item Nested groups: \code{(1 | a/b)} expands to \code{(1|a) + (1|a:b)}
\item Interactions: \code{(1 + x*z | group)} expands \code{x*z} to \code{x + z + x:z}
\item Polynomials: \code{(1 + poly(x,2) | group)} creates columns for each poly term
\item Inline transforms: \code{(1 + I(x^2) | group)} evaluates the transformation
}

The \code{||} syntax is equivalent to \code{(1 | group) + (0 + x | group)} but more concise.
}
\keyword{internal}
