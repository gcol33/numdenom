% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/prior_predict.R
\name{prior_predict}
\alias{prior_predict}
\title{Prior predictive simulation}
\usage{
prior_predict(formula, family, data, priors = NULL, n = 100)
}
\arguments{
\item{formula}{A model formula, using the combined \code{num | denom ~ x} syntax
or a single response with \code{formula_denom} supplied through
\code{\link[=ratiod_formula]{ratiod_formula()}}.}

\item{family}{A \code{ratiod_family} object.}

\item{data}{Data frame supplying the predictors, the grouping factors and,
for the binomial families, the trial counts. Response columns need not be
present; where one is missing a placeholder is used, since nothing about
the prior predictive distribution depends on the observed response.}

\item{priors}{A \code{ratiod_priors} object. Defaults to \code{ratiod_priors()}.}

\item{n}{Number of prior predictive draws.}
}
\value{
An object of class \code{ratiod_priorpred}: a list with the \code{n} x \code{N}
matrices \code{y_num}, \code{y_denom}, \code{ratio}, \code{mu_num} and \code{mu_denom}, the drawn
coefficients \code{beta_num} and \code{beta_denom}, the drawn random-effect scales
\code{sigma}, the drawn dispersions \code{phi_num} and \code{phi_denom}, and the \code{family}
and \code{data} the draws were taken on.
}
\description{
Simulate numerator and denominator responses from the prior predictive
distribution: parameters are drawn from \code{priors}, the two linear predictors
are assembled on the design \code{formula} and \code{data} imply, and a response is
drawn from \code{family} at each draw. Comparing the resulting ratios against
what is plausible for the system under study is the check on whether the
priors are reasonable before any data are fitted.

Random-effect terms contribute at a standard deviation drawn from
\code{priors$sigma}, one per intercept and one per slope column. Correlation
between slopes within a term is not drawn, so a correlated term simulates
at its marginal scales.
}
\examples{
set.seed(1)
df <- data.frame(
  count = rep(0L, 40),
  total = rep(0L, 40),
  x = rnorm(40),
  site = factor(rep(1:8, each = 5))
)

pp <- prior_predict(
  count | total ~ x + (1 | site),
  family = ratiod_negbin_negbin(),
  data = df,
  n = 50
)
dim(pp$ratio)
summary(as.vector(pp$ratio))

# A tighter coefficient prior narrows the predicted ratios
pp_tight <- prior_predict(
  count | total ~ x + (1 | site),
  family = ratiod_negbin_negbin(),
  data = df,
  priors = ratiod_priors(beta = prior_normal(0, 0.5)),
  n = 50
)

}
\seealso{
\code{\link[=sim_ratiod]{sim_ratiod()}} for simulating from a known parameterisation,
\code{\link[=ratiod_priors]{ratiod_priors()}} for the prior specification
}
