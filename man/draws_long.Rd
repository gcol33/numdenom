% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ratio.R
\name{draws_long}
\alias{draws_long}
\alias{draws_long.ratiod_fit}
\title{Draws for selected parameters, one row per parameter per draw}
\usage{
draws_long(object, ..., regex = FALSE, ndraws = NULL)

\method{draws_long}{ratiod_fit}(object, ..., regex = FALSE, ndraws = NULL)
}
\arguments{
\item{object}{A \code{ratiod_fit} object}

\item{...}{Parameter names to extract (unquoted).}

\item{regex}{Logical; if TRUE, treat parameter names as regex patterns.}

\item{ndraws}{Number of draws to return. If NULL, returns all.}
}
\value{
A data frame with columns \code{.chain}, \code{.iteration}, \code{.draw},
\code{.variable}, and \code{.value}.
}
\description{
The long counterpart of \code{\link[=draws_wide]{draws_wide()}}: one row per draw per parameter,
with the parameter in \code{.variable} and its value in \code{.value}.
}
\examples{
\donttest{
# Generate synthetic data
set.seed(333)
n <- 55
df <- data.frame(
  count = rpois(n, lambda = 24),
  effort = rgamma(n, shape = 12, rate = 1),
  depth = rnorm(n),
  site = sample(letters[1:4], n, replace = TRUE)
)

# Fit model
fit <- tratio(
  count | effort ~ depth + (1 | site),
  data = df,
  family = ratiod_poisson_gamma(),
  mode = "hmc",
  control = list(iter = 200, warmup = 100, chains = 1)
)

# Gather all fixed effects
draws_long(fit, beta_num, beta_denom)

# Use for plotting
if (requireNamespace("ggplot2", quietly = TRUE)) {
  library(ggplot2)
  draws_long(fit, beta_num) |>
    ggplot(aes(x = .value)) +
    geom_histogram() +
    facet_wrap(~ .variable)
}
}

}
