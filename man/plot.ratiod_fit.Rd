% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/tratio.R
\name{plot.ratiod_fit}
\alias{plot.ratiod_fit}
\title{Plot method for ratiod_fit}
\usage{
\method{plot}{ratiod_fit}(x, pars = NULL, type = c("trace", "dens", "both"), n_col = NULL, ...)
}
\arguments{
\item{x}{A ratiod_fit object}

\item{pars}{Character vector of parameter names to plot. If NULL (default),
plots main parameters (fixed effects, variance components).}

\item{type}{Type of plot: "trace" (default), "dens" (density), or "both".}

\item{n_col}{Number of columns in the plot grid (default: auto).}

\item{...}{Additional arguments (currently ignored).}
}
\value{
Invisibly returns the plot object (base graphics or ggplot2).
}
\description{
Creates diagnostic plots for ratio model fits including trace plots
and optional density plots. Works with all backends (HMC, PG, Laplace).
}
\examples{
# Create simple dataset
set.seed(456)
df <- data.frame(
  count = rpois(40, 8),
  effort = rgamma(40, 2, 0.5),
  depth = rnorm(40),
  site = factor(sample(1:4, 40, replace = TRUE))
)

\donttest{
fit <- tratio(
  count | effort ~ depth + (1|site),
  data = df,
  family = ratiod_poisson_gamma(),
  control = list(iter = 200, warmup = 100, chains = 2)
)
plot(fit)
plot(fit, pars = "beta_num", type = "both")
}

}
