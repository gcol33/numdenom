% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ratio.R
\name{fitted.ratiod_fit}
\alias{fitted.ratiod_fit}
\title{Extract fitted values from a ratio model}
\usage{
\method{fitted}{ratiod_fit}(
  object,
  component = c("all", "numerator", "denominator", "ratio"),
  summary = TRUE,
  probs = c(0.025, 0.5, 0.975),
  ...
)
}
\arguments{
\item{object}{A \code{ratiod_fit} object}

\item{component}{Which component to return: "numerator", "denominator",
"ratio", or "all" (default).}

\item{summary}{Logical; if TRUE, return summary statistics instead of
full posterior draws (default TRUE).}

\item{probs}{Quantiles to compute if \code{summary = TRUE}.}

\item{...}{Ignored}
}
\value{
If \code{summary = TRUE}, a data frame with posterior summaries.
If \code{summary = FALSE}, a list with matrices of posterior draws.
}
\description{
Compute posterior distributions of fitted values (expected values) for both
the numerator and denominator processes from a fitted ratio model.
}
\examples{
\donttest{
# Generate synthetic data
set.seed(789)
n <- 40
df <- data.frame(
  count = rpois(n, lambda = 15),
  effort = rgamma(n, shape = 8, rate = 1),
  depth = rnorm(n)
)

# Fit model
fit <- tratio(
  count | effort ~ depth,
  data = df,
  family = ratiod_poisson_gamma(),
  mode = "hmc",
  control = list(iter = 200, warmup = 100, chains = 1)
)

# Get fitted value summaries
fitted(fit)

# Get just numerator fitted values
fitted(fit, component = "numerator")

# Get full posterior draws
fitted(fit, summary = FALSE)
}

}
