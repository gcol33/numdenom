% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/simulate.R
\name{sim_ratiod_sbc}
\alias{sim_ratiod_sbc}
\title{Simulate multiple datasets for SBC}
\usage{
sim_ratiod_sbc(
  n_sims = 100,
  n = 100,
  family = ratiod_negbin_negbin(),
  priors = NULL,
  n_groups = 10,
  seed = NULL
)
}
\arguments{
\item{n_sims}{Number of simulations to run.}

\item{n}{Number of observations per simulation.}

\item{family}{A ratiod family object.}

\item{priors}{Prior specification for parameter generation.}

\item{n_groups}{Number of random effect groups.}

\item{seed}{Random seed.}
}
\value{
A list of \code{ratiod_simdata} objects.
}
\description{
Generate multiple simulated datasets for simulation-based calibration (SBC).
Each dataset is generated with parameters drawn from the prior.
}
\examples{
# Generate datasets for SBC (returns immediately)
sims <- sim_ratiod_sbc(
  n_sims = 3,
  n = 30,
  family = ratiod_negbin_negbin(),
  seed = 42
)
print(sims)

\donttest{
# Fit model to each simulation (slow, not run on CRAN)
# ranks <- lapply(sims, function(sim) {
#   fit <- ratiod(y_num | y_denom ~ x1 + (1 | group),
#                data = sim$data,
#                family = ratiod_negbin_negbin(),
#                iter = 200, warmup = 100, chains = 1)
#   sum(as.matrix(fit$draws)[, "beta_num[1]"] < sim$true_params$beta_num[1])
# })
}

}
\seealso{
\code{\link[=sim_ratiod]{sim_ratiod()}} for single dataset simulation
}
