% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/spatiotemporal.R
\name{st_has_rho}
\alias{st_has_rho}
\title{Does a spatiotemporal interaction estimate a time-margin correlation?}
\usage{
st_has_rho(st_info)
}
\arguments{
\item{st_info}{Output of \code{\link[=prepare_spatiotemporal_for_hmc]{prepare_spatiotemporal_for_hmc()}}.}
}
\value{
\code{TRUE} if the interaction carries a \code{logit_rho_st} parameter.
}
\description{
Type II applies the temporal precision within each spatial unit, Type IV
carries it as a Kronecker margin and HSGP-ST reads it per basis function, so
all three estimate an AR1 \code{rho}. Type I is iid over the whole grid and Type
III's time margin is unstructured, so neither has one; the GP types carry a
range instead. This mirrors \code{st_time_margin_is_structured()} in
\code{src/hmc_spatiotemporal.h}, which the parameter layout reads -- a parameter
allocated on one side and not the other shifts every index after it.
}
\keyword{internal}
