% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ratio.R
\name{predict_gibbs}
\alias{predict_gibbs}
\title{Predict for Gibbs backend}
\usage{
predict_gibbs(
  object,
  newdata,
  type,
  re_formula,
  allow_new_levels,
  include_spatial,
  return_spatial
)
}
\description{
Posterior predictive draws for the Gibbs ICAR/BYM2 spatial sampler.
Mirrors \code{predict_pg}: builds the design matrix for \code{newdata}, looks up
each new row's spatial group against the levels stored in the fit, and
assembles \verb{eta = X beta + phi[group]} per draw. For binomial families
(the supported case under the gibbs backend), the response-scale
prediction is \code{plogis(eta)}. Continuous-denominator families
(poisson_gamma, negbin_gamma) and count-denominator families
(negbin_negbin) add \verb{eta_denom = X_denom beta_denom + phi[group]} and
return \code{mu_num / mu_denom} on the response scale.
}
\keyword{internal}
