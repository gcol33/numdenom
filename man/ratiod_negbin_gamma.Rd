% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/family.R
\name{ratiod_negbin_gamma}
\alias{ratiod_negbin_gamma}
\title{Negative binomial - Gamma family for overdispersed count/effort ratios}
\usage{
ratiod_negbin_gamma(link_num = "log", link_denom = "log")
}
\arguments{
\item{link_num}{Link function for count rate (default: "log")}

\item{link_denom}{Link function for effort mean (default: "log")}
}
\value{
A \code{ratiod_family} object
}
\description{
Two-process model where the numerator follows a negative binomial distribution
and the denominator (effort/exposure) follows a Gamma distribution. This fills
the gap between Poisson-Gamma (equidispersed counts) and NegBin-NegBin
(integer denominator): use it when counts are overdispersed and effort is
continuous.

Use cases:
\itemize{
\item Overdispersed catch per unit effort (CPUE)
\item Overdispersed observations per continuous exposure
\item Any ratio of overdispersed counts over continuous positive effort
}
}
\examples{
# Create family object
fam <- ratiod_negbin_gamma()
print(fam)

# Simulate overdispersed CPUE data
set.seed(123)
n <- 60
df <- data.frame(
  catch = rnbinom(n, size = 5, mu = 8),
  effort_hours = rgamma(n, shape = 4, rate = 1),
  depth = rnorm(n),
  vessel = factor(rep(1:6, each = n/6))
)

\dontrun{
# Fit model (slow, not run on CRAN)
fit <- tratio(
  catch | effort_hours ~ depth + (1 | vessel),
  data = df,
  family = ratiod_negbin_gamma(),
  control = list(iter = 200, warmup = 100, chains = 1)
)
}

}
