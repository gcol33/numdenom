% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/family.R
\name{ratiod_binomial}
\alias{ratiod_binomial}
\title{Binomial family for trial-based ratios}
\usage{
ratiod_binomial(link = "logit", denominator_known = TRUE)
}
\arguments{
\item{link}{Link function for probability (default: "logit")}

\item{denominator_known}{Logical; if TRUE, denominator is treated as
fixed and known. If FALSE, denominator is also modelled.}
}
\value{
A \code{ratiod_family} object
}
\description{
Trial-based model where the numerator represents successes out of
denominator trials. The denominator can be treated as known (fixed)
or modelled with its own process.

Use cases:
\itemize{
\item Detection / availability (occupancy-style)
\item Successes / trials (hierarchical binomial)
\item Proportions with known denominators
}
}
\examples{
# Create family object
fam <- ratiod_binomial()
print(fam)

# Simulate detection/availability data
set.seed(123)
n <- 50
df <- data.frame(
  detections = rbinom(n, size = 10, prob = 0.4),
  trials = rep(10L, n),
  effort = rnorm(n),
  site = factor(rep(1:10, each = 5))
)

\dontrun{
# Fit model (slow, not run on CRAN)
fit <- ratiod(
  detections | trials ~ effort + (1 | site),
  data = df,
  family = ratiod_binomial(),
  iter = 200, warmup = 100, chains = 1
)
}

}
