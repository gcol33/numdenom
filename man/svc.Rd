% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/spatial.R
\name{svc}
\alias{svc}
\alias{svc.ratiod_fit}
\title{Extract spatially-varying coefficients from a fitted model}
\usage{
svc(object, terms = NULL, summary = FALSE, probs = c(0.025, 0.5, 0.975), ...)

\method{svc}{ratiod_fit}(object, terms = NULL, summary = FALSE, probs = c(0.025, 0.5, 0.975), ...)
}
\arguments{
\item{object}{A \code{ratiod_fit} object fitted with \code{svc} argument}

\item{terms}{Which SVC terms to extract. If NULL (default), extracts all.}

\item{summary}{Logical; if TRUE, return summary statistics instead of
full posterior draws.}

\item{probs}{Quantiles to compute if \code{summary = TRUE}.}

\item{...}{Ignored}
}
\value{
A \code{ratiod_svc_posterior} object containing:
\itemize{
\item \code{draws}: Array of posterior draws (draws x locations x terms)
\item \code{coords}: Coordinate matrix
\item \code{term_names}: Names of SVC terms
}
}
\description{
Extract posterior distributions of spatially-varying coefficients (SVCs)
from a fitted ratio model with SVC specification.
}
\examples{
\dontrun{
# Generate synthetic spatial data (not run - SVC not fully supported)
set.seed(303)
n <- 40
df <- data.frame(
  lon = runif(n, 0, 10),
  lat = runif(n, 0, 10),
  depth = rnorm(n),
  count = rpois(n, 20),
  effort = rgamma(n, shape = 4, rate = 1)
)

# Fit model with SVC
fit <- tratio(
  count | effort ~ depth,
  data = df,
  family = ratiod_poisson_gamma(),
  svc = spatial_svc(~ lon + lat, terms = c(1, 2)),
  control = list(iter = 200, warmup = 100, chains = 1)
)

# Extract SVC posteriors
svc_post <- svc(fit)
summary(svc_post)
}

}
\seealso{
\code{\link[=spatial_svc]{spatial_svc()}}, \code{\link[=plot.ratiod_svc_posterior]{plot.ratiod_svc_posterior()}}
}
