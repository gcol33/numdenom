% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/family_zi.R
\name{ratiod_zipois}
\alias{ratiod_zipois}
\title{Zero-inflated Poisson family}
\usage{
ratiod_zipois(
  link_num = "log",
  link_denom = "log",
  link_zi = "logit",
  denom_family = "gamma"
)
}
\arguments{
\item{link_num}{Link function for count mean (default: "log")}

\item{link_denom}{Link function for denominator mean (default: "log")}

\item{link_zi}{Link function for zero-inflation probability (default: "logit")}

\item{denom_family}{Denominator distribution: "gamma" (default), "negbin", or "fixed"}
}
\value{
A \code{ratiod_family} object with zero-inflation
}
\description{
Zero-inflated Poisson for the numerator process.
Useful when overdispersion is primarily due to excess zeros.
}
\details{
Use \code{ratiod_zipois()} when:
\itemize{
\item Excess zeros are the main source of overdispersion
\item The non-zero counts follow Poisson (no additional overdispersion)
}

Use \code{ratiod_zinegbin()} when:
\itemize{
\item Both excess zeros AND overdispersion in positive counts
}
}
\examples{
# Create family object
fam <- ratiod_zipois()
print(fam)

# Simulate zero-inflated CPUE data
set.seed(123)
n <- 60
zi_prob <- 0.25
df <- data.frame(
  catch = ifelse(runif(n) < zi_prob, 0, rpois(n, lambda = 5)),
  effort = rgamma(n, shape = 4, rate = 1),
  depth = rnorm(n),
  vessel = factor(rep(1:6, each = n/6))
)

\dontrun{
# Fit model (not run - ZI models require specialized backend support)
fit <- tratio(
  catch | effort ~ depth + (1 | vessel),
  data = df,
  family = ratiod_zipois(denom_family = "gamma"),
  iter = 200, warmup = 100, chains = 1
)
}

}
