% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/family.R
\name{ratiod_negbin_negbin}
\alias{ratiod_negbin_negbin}
\title{Negative binomial - Negative binomial family}
\usage{
ratiod_negbin_negbin(link_num = "log", link_denom = "log")
}
\arguments{
\item{link_num}{Link function for numerator (default: "log")}

\item{link_denom}{Link function for denominator (default: "log")}
}
\value{
A \code{ratiod_family} object
}
\description{
Two-process model where both numerator and denominator follow negative
binomial distributions. This is the core family for count-count ratios
where both processes exhibit overdispersion.

Use cases:
\itemize{
\item Species counts / total counts (relative abundance)
\item Events / opportunities (both counts)
\item Any ratio where numerator and denominator are overdispersed counts
}
}
\examples{
# Create family object
fam <- ratiod_negbin_negbin()
print(fam)

# Simulate data for relative abundance
set.seed(123)
n <- 60
df <- data.frame(
  sp_count = rnbinom(n, size = 5, mu = 15),
  total_count = rnbinom(n, size = 8, mu = 100),
  habitat = factor(rep(c("forest", "grassland"), each = n/2)),
  site = factor(rep(1:10, each = n/10))
)

\dontrun{
# Fit model (slow, not run on CRAN)
fit <- tratio(
  sp_count | total_count ~ habitat + (1 | site),
  data = df,
  family = ratiod_negbin_negbin(),
  control = list(iter = 200, warmup = 100, chains = 1)
)
}

}
