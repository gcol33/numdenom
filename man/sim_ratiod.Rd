% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/simulate.R
\name{sim_ratiod}
\alias{sim_ratiod}
\title{Simulate data from ratio models}
\usage{
sim_ratiod(
  n = 100,
  family = ratiod_negbin_negbin(),
  formula = NULL,
  beta_num = NULL,
  beta_denom = NULL,
  sigma_re = 0.5,
  phi_num = 5,
  phi_denom = 5,
  n_groups = 10,
  n_per_group = NULL,
  spatial = NULL,
  seed = NULL
)
}
\arguments{
\item{n}{Number of observations to simulate.}

\item{family}{A tulpaRatio family object specifying the distributions.}

\item{formula}{Optional formula for generating covariates and structure.
If NULL, generates intercept-only data.}

\item{beta_num}{Numeric vector of true numerator coefficients.
If NULL, drawn from prior.}

\item{beta_denom}{Numeric vector of true denominator coefficients.
If NULL, drawn from prior.}

\item{sigma_re}{Standard deviation of random effects. Default 0.5.}

\item{phi_num}{Overdispersion for numerator (negbin/gamma families). Default 5.}

\item{phi_denom}{Overdispersion for denominator (negbin/gamma families). Default 5.}

\item{n_groups}{Number of random effect groups. Default 10.}

\item{n_per_group}{Observations per group. If NULL, groups are unbalanced.}

\item{spatial}{Optional spatial structure specification.}

\item{seed}{Random seed for reproducibility.}
}
\value{
A list with class \code{ratiod_simdata} containing:
\describe{
\item{data}{Data frame with simulated observations}
\item{true_params}{List of true parameter values used}
\item{family}{Family specification}
\item{n}{Number of observations}
}
}
\description{
Generate simulated datasets from prior or posterior predictive distributions.
Useful for prior predictive checks, simulation-based calibration, and
understanding model behavior.
}
\details{
The simulation generates data according to the ratio model structure:

For \strong{negbin_negbin}:
\itemize{
\item \code{y_num ~ NegBin(mu_num, phi_num)}
\item \code{y_denom ~ NegBin(mu_denom, phi_denom)}
\item \code{log(mu_num) = X \%*\% beta_num + re}
\item \code{log(mu_denom) = X \%*\% beta_denom + re} (shared RE)
}

For \strong{binomial}:
\itemize{
\item \code{y_num ~ Binomial(y_denom, p)}
\item \code{logit(p) = X \%*\% beta + re}
}

For \strong{poisson_gamma}:
\itemize{
\item \code{y_num ~ Poisson(mu_num)}
\item \code{y_denom ~ Gamma(shape, rate)} where \code{rate = shape / mu_denom}
}
}
\examples{
# Simulate CPUE-type data
sim <- sim_ratiod(
  n = 200,
  family = ratiod_poisson_gamma(),
  beta_num = c(2, 0.5),      # intercept + depth effect
  beta_denom = c(1, 0.2),    # effort model
  sigma_re = 0.3,
  n_groups = 20,
  seed = 123
)

# View simulated data
head(sim$data)

# Check true ratio distribution
true_ratio <- sim$data$y_num / sim$data$y_denom
hist(true_ratio, main = "Simulated ratios")

# Simulate binomial proportion data
sim_binom <- sim_ratiod(
  n = 100,
  family = ratiod_binomial(),
  beta_num = c(-0.5, 1),     # logit-scale coefficients
  n_groups = 10,
  seed = 456
)

\donttest{
# Fit model to simulated data (slow, not run on CRAN)
fit <- tratio(
  y_num | y_denom ~ x1 + (1 | group),
  data = sim$data,
  family = ratiod_poisson_gamma(),
  iter = 200, warmup = 100, chains = 1
)
# Compare estimated vs. true parameters
# summary(fit)
# sim$true_params
}

}
\seealso{
\code{\link[=tratio]{tratio()}} for model fitting, \code{\link[=prior_predict]{prior_predict()}} for prior checks
}
