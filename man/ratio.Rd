% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ratio.R
\name{ratio}
\alias{ratio}
\title{Extract ratio posteriors from a ratiod model}
\usage{
ratio(
  object,
  newdata = NULL,
  type = c("response", "log", "logit"),
  by = NULL,
  summary = FALSE,
  probs = c(0.025, 0.5, 0.975),
  ...
)
}
\arguments{
\item{object}{A \code{ratiod_fit} object}

\item{newdata}{Optional data frame for prediction. If NULL, uses
original data.}

\item{type}{Scale for ratio: "response" (default), "log", or "logit".}

\item{by}{Optional grouping variable name to aggregate ratios by group.}

\item{summary}{Logical; if TRUE, return summary statistics instead
of full posterior draws.}

\item{probs}{Quantiles to compute if \code{summary = TRUE}.}

\item{...}{Ignored}
}
\value{
A \code{ratiod_ratio} object containing posterior draws or summaries.
}
\description{
Compute posterior distributions of ratios from a fitted ratiod model.
Ratios are derived quantities: E[numerator] / E[denominator], computed
in Stan's generated quantities block with full uncertainty propagation.
}
\examples{
# Create a simple family
fam <- ratiod_poisson_gamma()

\donttest{
# Generate synthetic data
set.seed(123)
n <- 50
df <- data.frame(
  count = rpois(n, lambda = 20),
  effort = rgamma(n, shape = 10, rate = 1),
  depth = rnorm(n),
  site = sample(letters[1:5], n, replace = TRUE)
)

# Fit model
fit <- ratiod(
  count | effort ~ depth + (1 | site),
  data = df,
  family = ratiod_poisson_gamma(),
  backend = "hmc",
  iter = 200, warmup = 100, chains = 1
)

# Extract all observation-level ratios
r <- ratio(fit)
summary(r)

# Log-ratio scale
r_log <- ratio(fit, type = "log")

# Group-level ratios
r_site <- ratio(fit, by = "site")
}

}
