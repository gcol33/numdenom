% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/posterior_predict.R
\name{posterior_predict.ratiod_fit}
\alias{posterior_predict.ratiod_fit}
\title{Posterior predictive draws}
\usage{
\method{posterior_predict}{ratiod_fit}(object, ndraws = NULL, ...)
}
\arguments{
\item{object}{A \code{ratiod_fit} object}

\item{ndraws}{Number of posterior draws to replicate. \code{NULL} (the default)
uses every draw; a smaller number takes a random subsample.}

\item{...}{Unused.}
}
\value{
A list with \code{y_num_rep} and \code{y_denom_rep}, each a draws-by-observation
matrix, plus \code{y_num} and \code{y_denom}, the observed responses. A family with a
fixed denominator (the binomial arms, whose denominator is the trial count)
reports the trials as \code{y_denom_rep}.
}
\description{
Draw replicated numerator and denominator responses from the posterior
predictive distribution: at each posterior draw the two fitted means are
assembled from that draw's parameters, and a response is drawn from the
family at those means. The replicates are what \code{\link[=pp_check]{pp_check()}} compares the
observed data against.
}
\examples{
\donttest{
set.seed(4)
n <- 40
df <- data.frame(
  count = rpois(n, 12),
  total = rpois(n, 80),
  x = rnorm(n)
)
fit <- tratio(count | total ~ x, data = df,
              family = ratiod_poisson_gamma(),
              control = list(iter = 200, warmup = 100, chains = 1))
rep <- posterior_predict(fit, ndraws = 50)
dim(rep$y_num_rep)
}

}
\seealso{
\code{\link[=pp_check]{pp_check()}}
}
