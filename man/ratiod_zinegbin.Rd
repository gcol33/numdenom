% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/family_zi.R
\name{ratiod_zinegbin}
\alias{ratiod_zinegbin}
\title{Zero-inflated negative binomial family}
\usage{
ratiod_zinegbin(
  link_num = "log",
  link_denom = "log",
  link_zi = "logit",
  denom_family = "negbin"
)
}
\arguments{
\item{link_num}{Link function for count mean (default: "log")}

\item{link_denom}{Link function for denominator mean (default: "log")}

\item{link_zi}{Link function for zero-inflation probability (default: "logit")}

\item{denom_family}{Denominator distribution: "negbin" (default) or "fixed"}
}
\value{
A \code{ratiod_family} object with zero-inflation
}
\description{
Zero-inflated negative binomial for the numerator process.
Models excess zeros as a mixture:

\deqn{P(Y = 0) = \pi + (1 - \pi) \cdot P_{NB}(0)}
\deqn{P(Y = y) = (1 - \pi) \cdot P_{NB}(y), \quad y > 0}

where \eqn{\pi} is the zero-inflation probability and
\eqn{P_{NB}} is the negative binomial PMF.
}
\details{
The zero-inflation probability can have its own linear predictor via the
\code{zi} argument in \code{ratiod()}. If not specified, a single intercept is estimated.

For ecological data, zero-inflation often represents:
\itemize{
\item Structural zeros (species truly absent vs not detected)
\item Sampling zeros (inadequate effort)
\item False negatives
}
}
\examples{
# Create family object
fam <- ratiod_zinegbin()
print(fam)

# Simulate zero-inflated count data
set.seed(123)
n <- 60
zi_prob <- 0.3
df <- data.frame(
  count = ifelse(runif(n) < zi_prob, 0, rnbinom(n, size = 3, mu = 8)),
  total = rnbinom(n, size = 5, mu = 50),
  habitat = factor(rep(c("forest", "grassland"), each = n/2)),
  site = factor(rep(1:10, each = n/10))
)

\dontrun{
# Fit model (not run - ZI models require specialized backend support)
fit <- ratiod(
  count | total ~ habitat + (1 | site),
  data = df,
  family = ratiod_zinegbin(),
  iter = 200, warmup = 100, chains = 1
)
}

}
\seealso{
\code{\link[=ratiod_hurdle_negbin]{ratiod_hurdle_negbin()}} for hurdle model alternative
}
