% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ratio.R
\name{draws_wide}
\alias{draws_wide}
\alias{draws_wide.ratiod_fit}
\title{Draws for selected parameters, one column per parameter}
\usage{
draws_wide(object, ..., regex = FALSE, ndraws = NULL)

\method{draws_wide}{ratiod_fit}(object, ..., regex = FALSE, ndraws = NULL)
}
\arguments{
\item{object}{A \code{ratiod_fit} object}

\item{...}{Parameter names to extract (unquoted). Supports patterns like
\code{beta_num} or \code{beta_num[i]} for indexed parameters.}

\item{regex}{Logical; if TRUE, treat parameter names as regex patterns.}

\item{ndraws}{Number of draws to return. If NULL, returns all.}
}
\value{
A data frame with columns \code{.chain}, \code{.iteration}, \code{.draw}, and
one column per requested parameter.
}
\description{
Extract posterior draws for the named parameters into a wide data frame,
one row per draw and one column per parameter, alongside the \code{.chain},
\code{.iteration} and \code{.draw} bookkeeping columns.

Parameters are named unquoted, and a name matches every indexed element it
prefixes, so \code{beta_num} reaches \code{beta_num[1]}, \code{beta_num[2]} and so on.
}
\examples{
\donttest{
# Generate synthetic data
set.seed(222)
n <- 50
df <- data.frame(
  count = rpois(n, lambda = 20),
  effort = rgamma(n, shape = 10, rate = 1),
  depth = rnorm(n),
  site = sample(letters[1:5], n, replace = TRUE)
)

# Fit model
fit <- tratio(
  count | effort ~ depth + (1 | site),
  data = df,
  family = ratiod_poisson_gamma(),
  mode = "hmc",
  control = list(iter = 200, warmup = 100, chains = 1)
)

# Extract fixed effects
draws_wide(fit, beta_num, beta_denom)

# Subsample draws
draws_wide(fit, sigma_re, ndraws = 100)
}

}
