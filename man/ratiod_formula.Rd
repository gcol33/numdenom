% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/formula.R
\name{ratiod_formula}
\alias{ratiod_formula}
\title{Parse and validate tulpaRatio formulas}
\usage{
ratiod_formula(
  formula,
  formula_num = NULL,
  formula_denom = NULL,
  shared = NULL,
  data
)
}
\arguments{
\item{formula}{Main formula. Can be:
\itemize{
\item Combined: \code{num | denom ~ predictors}
\item Numerator only: \code{num ~ predictors} (requires formula_denom)
}}

\item{formula_num}{Optional formula for numerator-specific predictors.
Added to those from the main formula.}

\item{formula_denom}{Optional formula for denominator-specific predictors.
Required if main formula has single response.}

\item{shared}{Formula for shared random effects. NULL = infer from formulas,
\code{~ 0} = independence (triggers warning).}

\item{data}{Data frame containing all variables.}
}
\value{
A \code{ratiod_formula} object
}
\description{
Internal functions for parsing tulpaRatio formulas into a structured
specification for Stan model building.

tulpaRatio supports a combined formula syntax:
\itemize{
\item \code{num | denom ~ x + (1|group)} for shared predictors
\item Separate \code{formula_num} and \code{formula_denom} for different predictors
}

Parses the main formula and optional process-specific formulas into
a structured specification. Supports both combined syntax
(\code{num | denom ~ x}) and separate formulas.
}
\keyword{internal}
