% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/backend_hmc.R
\name{extract_re_for_hmc}
\alias{extract_re_for_hmc}
\title{Extract RE info for HMC}
\usage{
extract_re_for_hmc(formula)
}
\arguments{
\item{formula}{A ratiod_formula object}
}
\value{
List with RE structure for HMC backend
}
\description{
Extracts random effects information for the HMC backend.
Supports:
\itemize{
\item Multiple crossed random effects: \code{(1|site) + (1|year)}
\item Correlated random slopes: \code{(1 + x | group)} - estimates intercept-slope correlation
\item Uncorrelated random slopes: \code{(1 + x || group)} - no correlation estimated
}

For correlated slopes, the covariance matrix is parameterized via Cholesky decomposition:
\verb{Sigma = diag(sigma) * L * L' * diag(sigma)} where L is lower-triangular with unit diagonal
(LKJ correlation prior).
}
\keyword{internal}
