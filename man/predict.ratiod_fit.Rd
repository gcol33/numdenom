% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ratio.R
\name{predict.ratiod_fit}
\alias{predict.ratiod_fit}
\title{Predict method for ratiod models}
\usage{
\method{predict}{ratiod_fit}(
  object,
  newdata,
  type = c("response", "link"),
  component = c("ratio", "numerator", "denominator", "all"),
  summary = TRUE,
  probs = c(0.025, 0.5, 0.975),
  re_formula = NULL,
  allow_new_levels = FALSE,
  ...
)
}
\arguments{
\item{object}{A \code{ratiod_fit} object}

\item{newdata}{Data frame with new observations. Must contain all
predictor variables used in the original model.}

\item{type}{Type of prediction: "response" for fitted values on
the response scale, "link" for linear predictor scale.}

\item{component}{Which component to predict: "ratio" (default),
"numerator", "denominator", or "all".}

\item{summary}{Logical; if TRUE, return summary statistics instead of
full posterior draws (default TRUE).}

\item{probs}{Quantiles to compute if \code{summary = TRUE}.}

\item{re_formula}{Formula for random effects in prediction.
Use \code{NULL} to include all random effects (if new levels match),
\code{NA} or \code{~ 0} to exclude random effects (population-level prediction).}

\item{allow_new_levels}{Logical; if TRUE, allows new group levels not
in the original data (predictions use population mean).}

\item{...}{Ignored}
}
\value{
If \code{summary = TRUE}, a data frame with posterior summaries.
If \code{summary = FALSE}, a matrix of posterior draws (rows = draws, cols = obs).
}
\description{
Compute posterior predictions for new data from a fitted ratiod model.
}
\examples{
\dontrun{
# Generate synthetic data
set.seed(321)
n <- 50
df <- data.frame(
  count = rpois(n, lambda = 18),
  effort = rgamma(n, shape = 9, rate = 1),
  depth = rnorm(n),
  season = sample(c("summer", "winter"), n, replace = TRUE)
)

# Fit model
fit <- ratiod(
  count | effort ~ depth + season,
  data = df,
  family = ratiod_poisson_gamma(),
  backend = "hmc",
  iter = 200, warmup = 100, chains = 1
)

# Predict for new data
new_df <- data.frame(depth = c(10, 20, 30), season = "summer")
predict(fit, newdata = new_df)

# Population-level prediction (no random effects)
predict(fit, newdata = new_df, re_formula = NA)

# Get full posterior draws
predict(fit, newdata = new_df, summary = FALSE)
}

}
