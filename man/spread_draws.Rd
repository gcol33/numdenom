% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ratio.R
\name{spread_draws}
\alias{spread_draws}
\alias{spread_draws.ratiod_fit}
\title{Spread draws into wide format (tidybayes-style)}
\usage{
spread_draws(object, ..., regex = FALSE, ndraws = NULL)

\method{spread_draws}{ratiod_fit}(object, ..., regex = FALSE, ndraws = NULL)
}
\arguments{
\item{object}{A \code{ratiod_fit} object}

\item{...}{Parameter names to extract (unquoted). Supports patterns like
\code{beta_num} or \code{beta_num[i]} for indexed parameters.}

\item{regex}{Logical; if TRUE, treat parameter names as regex patterns.}

\item{ndraws}{Number of draws to return. If NULL, returns all.}
}
\value{
A data frame with columns \code{.chain}, \code{.iteration}, \code{.draw}, and
one column per requested parameter.
}
\description{
Extract posterior draws for specified parameters and spread them into
a wide format data frame. This function provides tidybayes-style syntax
without requiring the tidybayes package.
}
\examples{
\donttest{
# Generate synthetic data
set.seed(222)
n <- 50
df <- data.frame(
  count = rpois(n, lambda = 20),
  effort = rgamma(n, shape = 10, rate = 1),
  depth = rnorm(n),
  site = sample(letters[1:5], n, replace = TRUE)
)

# Fit model
fit <- tratio(
  count | effort ~ depth + (1 | site),
  data = df,
  family = ratiod_poisson_gamma(),
  backend = "hmc",
  iter = 200, warmup = 100, chains = 1
)

# Extract fixed effects
spread_draws(fit, beta_num, beta_denom)

# Subsample draws
spread_draws(fit, sigma_re, ndraws = 100)
}

}
