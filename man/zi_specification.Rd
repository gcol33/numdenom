% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/zi.R
\name{zi_specification}
\alias{zi_specification}
\title{Zero-Inflation and Hurdle Model Specifications}
\description{
Functions to specify zero-inflation or hurdle model components for ratiod models.
These handle excess zeros in count data where the standard count distribution
(Poisson, negative binomial) doesn't adequately capture the zero proportion.
}
\details{
\strong{Zero-Inflated Models:}
Zero-inflated models assume two data-generating processes:
\enumerate{
\item A process that generates "structural zeros" with probability \eqn{\pi}
\item A count process that generates counts (including zeros) with probability \eqn{1-\pi}
}

The probability of observing y is:
\deqn{P(Y=0) = \pi + (1-\pi) \cdot P_{count}(0)}
\deqn{P(Y=y|y>0) = (1-\pi) \cdot P_{count}(y)}

\strong{Hurdle Models:}
Hurdle models treat the process as:
\enumerate{
\item A binary process determining zero vs non-zero
\item A truncated count process (conditional on y > 0)
}

\deqn{P(Y=0) = 1 - \theta}
\deqn{P(Y=y|y>0) = \theta \cdot P_{count}(y|y>0)}

\strong{When to use:}
\itemize{
\item \code{zi_poisson()}: Excess zeros with equidispersed positive counts
\item \code{zi_negbin()}: Excess zeros with overdispersed positive counts
\item \code{hurdle_poisson()}: Zero hurdle with equidispersed positive counts
\item \code{hurdle_negbin()}: Zero hurdle with overdispersed positive counts
}
}
\keyword{models}
