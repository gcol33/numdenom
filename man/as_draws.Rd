% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ratio.R
\name{as_draws}
\alias{as_draws}
\alias{as_draws.ratiod_fit}
\title{Convert ratiod_fit to posterior draws format}
\usage{
as_draws(x, ...)

\method{as_draws}{ratiod_fit}(x, ...)
}
\arguments{
\item{x}{A \code{ratiod_fit} object}

\item{...}{Additional arguments passed to \code{posterior::as_draws_df}}
}
\value{
A \code{draws_df} object from the posterior package.
}
\description{
Convert a fitted ratiod model to a format compatible with the
posterior and tidybayes packages. Returns draws in a tidy format
suitable for further analysis and visualization.
}
\details{
The returned draws object is compatible with:
\itemize{
\item \strong{posterior} package functions (\code{summarise_draws}, \code{rvar}, etc.)
\item \strong{tidybayes} package functions (\code{spread_draws}, \code{gather_draws}, etc.)
\item \strong{bayesplot} package visualization functions
}

For tidybayes integration, use this function to extract draws, then
apply tidybayes functions for posterior analysis.
}
\examples{
\donttest{
# Generate synthetic data
set.seed(111)
n <- 45
df <- data.frame(
  count = rpois(n, lambda = 22),
  effort = rgamma(n, shape = 11, rate = 1),
  depth = rnorm(n),
  site = sample(letters[1:4], n, replace = TRUE)
)

# Fit model
fit <- ratiod(
  count | effort ~ depth + (1 | site),
  data = df,
  family = ratiod_poisson_gamma(),
  backend = "hmc",
  iter = 200, warmup = 100, chains = 1
)

# Convert to draws format
draws <- as_draws(fit)

# Use with posterior package
if (requireNamespace("posterior", quietly = TRUE)) {
  posterior::summarise_draws(draws)
}
}

}
\seealso{
\code{\link[posterior:as_draws_df]{posterior::as_draws_df()}}, \code{\link[=ratio]{ratio()}} for ratio-specific extraction
}
