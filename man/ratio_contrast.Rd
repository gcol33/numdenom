% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ratio.R
\name{ratio_contrast}
\alias{ratio_contrast}
\title{Compute contrasts between ratio conditions}
\usage{
ratio_contrast(
  object,
  contrast,
  type = c("difference", "ratio"),
  ref = NULL,
  ...
)
}
\arguments{
\item{object}{A \code{ratiod_fit} object}

\item{contrast}{A formula specifying the contrast, e.g., \code{~ treatment}}

\item{type}{Type of contrast: "difference" (default) or "ratio"}

\item{ref}{Reference level for factor contrasts (default: first level)}

\item{...}{Ignored}
}
\value{
A data frame with posterior summaries of contrasts
}
\description{
Compute posterior distributions of contrasts (differences or ratios)
between conditions for the derived ratio quantity.
}
\examples{
\donttest{
# Generate synthetic data with grouping variable
set.seed(456)
n <- 60
df <- data.frame(
  count = rpois(n, lambda = 25),
  effort = rgamma(n, shape = 12, rate = 1),
  season = sample(c("summer", "winter"), n, replace = TRUE)
)

# Fit model
fit <- ratiod(
  count | effort ~ season,
  data = df,
  family = ratiod_poisson_gamma(),
  backend = "hmc",
  iter = 200, warmup = 100, chains = 1
)

# Compare CPUE between seasons
ratio_contrast(fit, ~ season)

# Ratio of ratios (multiplicative comparison)
ratio_contrast(fit, ~ season, type = "ratio")
}

}
