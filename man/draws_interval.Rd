% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ratio.R
\name{draws_interval}
\alias{draws_interval}
\alias{draws_interval.ratiod_fit}
\title{Point estimates and credible intervals for selected parameters}
\usage{
draws_interval(
  object,
  ...,
  .width = 0.95,
  .point = c("median", "mean"),
  .interval = c("qi", "hdi")
)

\method{draws_interval}{ratiod_fit}(
  object,
  ...,
  .width = 0.95,
  .point = c("median", "mean"),
  .interval = c("qi", "hdi")
)
}
\arguments{
\item{object}{A \code{ratiod_fit} object}

\item{...}{Parameter names to summarize (unquoted).}

\item{.width}{Width(s) of credible intervals. Default 0.95.}

\item{.point}{Point estimate function: "median" (default) or "mean".}

\item{.interval}{Interval type: "qi" for quantile interval (default),
"hdi" for highest density interval.}
}
\value{
A data frame with columns for the variable name, point estimate,
and interval bounds.
}
\description{
Summarize the draws of the named parameters as a point estimate (median or
mean) and a credible interval (quantile or highest-density), one row per
parameter per interval width.
}
\examples{
\donttest{
# Generate synthetic data
set.seed(444)
n <- 40
df <- data.frame(
  count = rpois(n, lambda = 18),
  effort = rgamma(n, shape = 9, rate = 1),
  depth = rnorm(n),
  site = sample(letters[1:3], n, replace = TRUE)
)

# Fit model
fit <- tratio(
  count | effort ~ depth + (1 | site),
  data = df,
  family = ratiod_poisson_gamma(),
  mode = "hmc",
  control = list(iter = 200, warmup = 100, chains = 1)
)

# Get point estimates and 95\% intervals
draws_interval(fit, beta_num, beta_denom)

# Multiple interval widths
draws_interval(fit, beta_num, .width = c(0.5, 0.9, 0.95))
}

}
