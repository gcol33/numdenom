% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/family.R
\name{ratiod_beta_binomial}
\alias{ratiod_beta_binomial}
\title{Beta-binomial family for overdispersed proportions}
\usage{
ratiod_beta_binomial(link = "logit", denominator_known = TRUE)
}
\arguments{
\item{link}{Link function for probability (default: "logit")}

\item{denominator_known}{Logical; if TRUE, denominator is treated as
fixed and known. If FALSE, denominator is also modelled.}
}
\value{
A \code{ratiod_family} object
}
\description{
Two-process model where the numerator follows a beta-binomial distribution,
allowing for overdispersion in binomial data. The denominator is the number
of trials, which can be fixed or modelled.

Use cases:
\itemize{
\item Overdispersed proportions (e.g., infection rates across sites)
\item Hierarchical binomial with extra-binomial variation
\item Proportions with correlation within clusters
}
}
\details{
The beta-binomial distribution arises when the success probability
itself follows a beta distribution. This creates overdispersion
(variance > binomial variance) and is useful when:
\itemize{
\item There is unmodelled heterogeneity in success probability
\item Observations within clusters are correlated
\item The binomial assumption of independent trials is violated
}

The overdispersion parameter \eqn{\rho} (intra-class correlation)
ranges from 0 (binomial) to 1 (maximum overdispersion).
}
\examples{
# Create family object
fam <- ratiod_beta_binomial()
print(fam)

# Simulate overdispersed proportion data
set.seed(123)
n <- 40
df <- data.frame(
  infected = rbinom(n, size = 20, prob = rbeta(n, 2, 5)),
  tested = rep(20L, n),
  treatment = factor(rep(c("control", "treated"), each = n/2)),
  site = factor(rep(1:8, each = n/8))
)

\dontrun{
# Fit model (slow, not run on CRAN)
fit <- ratiod(
  infected | tested ~ treatment + (1 | site),
  data = df,
  family = ratiod_beta_binomial(),
  iter = 200, warmup = 100, chains = 1
)
}

}
