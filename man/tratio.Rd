% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/tratio.R
\name{tratio}
\alias{tratio}
\title{Fit a ratio, rate, or proportion model}
\usage{
tratio(
  formula,
  data,
  family = ratiod_negbin_negbin(),
  formula_num = NULL,
  formula_denom = NULL,
  shared = NULL,
  spatial = NULL,
  temporal = NULL,
  spatiotemporal = NULL,
  zi = NULL,
  latent = NULL,
  priors = NULL,
  mode = c("auto", "exact", "structured", "optimized", "hmc", "ess", "pg", "gibbs",
    "sghmc", "sgld", "laplace", "vi"),
  control = list()
)
}
\arguments{
\item{formula}{Model formula specifying the response and predictors.
Two syntax options: \strong{Combined} (recommended) \code{num | denom ~ predictors + (1|group)}
where both processes share the same predictors and random effects;
\strong{Separate} \code{num ~ predictors} with the \code{formula_denom} argument.}

\item{data}{Data frame containing all variables.}

\item{family}{A tulpaRatio family object specifying the distributions:
\code{\link[=ratiod_negbin_negbin]{ratiod_negbin_negbin()}} for both count processes (default),
\code{\link[=ratiod_binomial]{ratiod_binomial()}} for successes/trials,
\code{\link[=ratiod_poisson_gamma]{ratiod_poisson_gamma()}} for count/continuous effort (CPUE).}

\item{formula_num}{Optional one-sided formula for additional numerator
predictors: \code{~ extra_terms}. Added to the main formula.}

\item{formula_denom}{Optional formula for denominator. Required if main
formula has single response. Can be \code{denom ~ predictors}.}

\item{shared}{Formula specifying shared random effects structure:
\code{NULL} (default) infers from matching random effects in both processes;
\code{~ (1 | group)} for explicit shared structure;
\code{~ 0} for independence assumption (triggers warning).}

\item{spatial}{Optional spatial structure specification.
See \code{\link[=spatial_car]{spatial_car()}}, \code{\link[=spatial_bym2]{spatial_bym2()}}.}

\item{temporal}{Optional temporal structure specification.
See \code{\link[=temporal_rw1]{temporal_rw1()}}, \code{\link[=temporal_rw2]{temporal_rw2()}}, \code{\link[=temporal_ar1]{temporal_ar1()}}.}

\item{spatiotemporal}{Optional spatiotemporal interaction specification.
See \code{\link[=spatiotemporal]{spatiotemporal()}}.}

\item{zi}{Optional zero-inflation specification.
See \code{\link[=zi_poisson]{zi_poisson()}}, \code{\link[=zi_negbin]{zi_negbin()}}, \code{\link[=hurdle_poisson]{hurdle_poisson()}}, \code{\link[=hurdle_negbin]{hurdle_negbin()}}.}

\item{latent}{Optional latent factor specification for unmeasured confounders.
See \code{\link[=latent_factor]{latent_factor()}}.}

\item{priors}{Prior specification. See \code{\link[=ratiod_priors]{ratiod_priors()}}.}

\item{mode}{Inference mode or backend. Accepts either:

\strong{Tier names} (epistemic guarantees):
\itemize{
\item \code{"auto"} (default): Selects between Exact and Structured based on model.
\strong{Never} silently chooses Optimized.
\item \code{"exact"} (Tier 1): Asymptotically correct posterior inference.
Credible intervals are interpretable as posterior uncertainty.
\item \code{"structured"} (Tier 2): Accurate inference conditional on structural
assumptions (latent Gaussian, conditional independence).
\item \code{"optimized"} (Tier 3): No general correctness guarantee.
Point estimates usually good, uncertainty often underestimated.
\strong{Requires explicit opt-in.}
}

\strong{Backend names} (specific implementation):
\itemize{
\item \code{"hmc"} (Tier 1): Native HMC/NUTS sampler
\item \code{"ess"} (Tier 1): Elliptical Slice Sampling
\item \code{"pg"} (Tier 1): Pólya-Gamma Gibbs (binomial only)
\item \code{"sghmc"} (Tier 1): Stochastic Gradient HMC (large datasets)
\item \code{"sgld"} (Tier 1): Stochastic Gradient Langevin Dynamics (large datasets)
\item \code{"laplace"} (Tier 2): Laplace approximation
\item \code{"vi"} (Tier 3): Variational Inference
}

\strong{Structures each backend fits.} \code{"hmc"} fits every structure. The rest
carry a subset, and a model naming a structure its backend cannot fit is
refused with that structure named:
\itemize{
\item \code{"ess"}, \code{"sghmc"}, \code{"sgld"}, \code{"vi"}: \code{spatial}, \code{temporal}, \code{zi}
\item \code{"gibbs"}: \code{spatial}, and \code{temporal} as \code{\link[=temporal_tvc]{temporal_tvc()}}
\item \code{"pg"}, \code{"laplace"}: \code{spatial}, or \code{temporal} as
\code{\link[=temporal_multiscale]{temporal_multiscale()}}, one of the two per model
}

\code{"auto"} selects a backend that fits the whole model, so it reaches
\code{"laplace"} or \code{"gibbs"} only for models those carry.

See \code{\link[=inference_mode_info]{inference_mode_info()}} for details on the tier system.}

\item{control}{Named list of perf / numerical / tuning knobs. Statistical
arguments stay in the signature above; everything that tunes \emph{how} the
fit runs lives here. Unknown names are an error, not a silent no-op.

Sampling budget (all backends):
\itemize{
\item \code{chains}: number of MCMC chains (default 4).
\item \code{iter}: total iterations per chain (default 2000).
\item \code{warmup}: warmup iterations per chain (default \code{floor(iter / 2)}).
\item \code{thin}: thinning interval (default 1).
\item \code{cores}: cores for parallel chains (default \code{getOption("mc.cores", chains)}).
\item \code{seed}: random seed for reproducibility.
\item \code{verbose}: print progress messages during fitting (default \code{TRUE}).
}

NUTS / HMC (\code{mode = "hmc"}):
\itemize{
\item \code{adapt_delta}: target average acceptance probability for step-size
adaptation. Higher values give smaller steps, reducing divergences but
slowing sampling. \code{NULL} (default) selects on model complexity: 0.80
simple, 0.85 ICAR, 0.90 BYM2 and correlated random slopes. Range
0.80--0.99.
\item \code{max_treedepth}: maximum NUTS tree depth. \code{NULL} (default) uses 10.
Raise if you see many max-treedepth warnings; costs computation per
iteration.
\item \code{metric}: mass matrix. \code{"auto"} (default) picks on model complexity;
\code{"dense"} full matrix with Ledoit-Wolf shrinkage (correlated posteriors:
random slopes, BYM2, HSGP, TVC); \code{"diag"} diagonal (faster per step, may
need deeper trees); \code{"block_diag"} per parameter group. Auto falls back
to diagonal when p > 2000.
\item \code{riemannian}: enable per-trajectory SoftAbs metric retry on divergence,
computing a local Hessian-based metric and retrying. \code{NULL} (default)
enables for BYM2 and ICAR with a dense mass matrix. Costs about (p+1)
gradient evaluations per divergent trajectory only.
\item \code{gradient_mode}: \code{"auto"} (default) selects the fastest available;
\code{"H"} hand-coded analytical (fastest); \code{"A_r"} arena reverse-mode
autodiff (fast, O(N)); \code{"A"} forward-mode (O(p*N), thread-safe);
\code{"A_t"} tape-based reverse-mode (slow, legacy); \code{"N"} numerical finite
differences (slowest, always works).
\item \code{re_param}: random-effect parameterization. \code{"noncentered"} (default)
stores z ~ N(0, 1) and computes re = sigma * z (or diag(sigma) * L * z
when correlated), suiting weakly-informed effects or small groups;
\code{"centered"} stores re directly under re ~ N(0, sigma^2), suiting
strongly-informed effects or large groups.
\item \code{L}: fixed leapfrog steps. Default 0 leaves NUTS to choose.
}

Variational inference (\code{mode = "vi"}):
\itemize{
\item \code{vi_variant}: \code{"auto"} (default) selects on parameter count;
\code{"meanfield"} diagonal covariance (fastest); \code{"lowrank"} low-rank plus
diagonal (balanced); \code{"fullrank"} full Cholesky (best for small models).
}

Stochastic gradient backends (\code{mode = "sghmc"} / \code{"sgld"}):
\itemize{
\item \code{batch_size}: minibatch size (default \code{ceiling(sqrt(n))}).
\item \code{epsilon}: step size.
\item \code{alpha}: friction coefficient (\code{"sghmc"} only).
\item \code{schedule_a}, \code{schedule_b}, \code{schedule_gamma}, \code{use_schedule}: step-size
decay schedule (\code{"sgld"} only).
}}
}
\value{
A \code{ratiod_fit} object containing:
\describe{
\item{draws}{Posterior draws matrix}
\item{formula}{Parsed formula specification}
\item{family}{Model family}
\item{data}{Original data}
\item{backend}{Inference backend used}
\item{diagnostics}{MCMC diagnostics (if applicable)}
}
}
\description{
Fit a Bayesian hierarchical model for ratios, rates, or proportions.
The model jointly estimates the numerator and denominator processes
with optional shared latent structure.

\code{tratio()} is tulpaRatio's front door, the ratio-model member of the
\verb{tulpa*} family of fitting verbs (\code{\link[tulpa:tulpa]{tulpa::tulpa()}} for the engine,
\code{tulpaObs::tobs()} for observation processes).

\strong{Core principle:} Ratios are derived quantities, computed post hoc
from the posterior of the joint model. The ratio is never modelled directly.
}
\examples{
# Quick example: create model object (no fitting)
set.seed(123)
df <- data.frame(
  catch = rpois(50, 10),
  effort = rgamma(50, 2, 0.5),
  depth = rnorm(50),
  season = factor(sample(c("spring", "summer"), 50, replace = TRUE)),
  site = factor(sample(1:5, 50, replace = TRUE))
)

\donttest{
# CPUE example with combined formula
fit <- tratio(
  catch | effort ~ depth + season + (1 | site),
  data = df,
  family = ratiod_poisson_gamma(),
  control = list(iter = 200, warmup = 100, chains = 1)
)

# Different predictors for each process
fit2 <- tratio(
  catch | effort ~ (1 | site),
  formula_num = ~ depth + season,
  formula_denom = ~ season,
  data = df,
  family = ratiod_poisson_gamma(),
  control = list(iter = 200, warmup = 100, chains = 1)
)

# Extract ratio posteriors
cpue <- ratio(fit)
summary(cpue)

# Compare ratios between groups
ratio_contrast(fit, ~ season)
}

}
\seealso{
\itemize{
\item \code{\link[=ratio]{ratio()}} for extracting ratio posteriors
\item \code{\link[=ratio_contrast]{ratio_contrast()}} for comparing ratios between groups
\item \code{\link[=pp_check]{pp_check()}} for posterior predictive checks
\item \code{\link[loo:loo]{loo::loo()}} and \code{\link[loo:waic]{loo::waic()}} for model comparison
}
}
