% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/temporal.R
\name{temporal_iid}
\alias{temporal_iid}
\title{IID temporal structure (Independent Effects)}
\usage{
temporal_iid(time_var, group_var = NULL, shared = TRUE)
}
\arguments{
\item{time_var}{Name of the time variable in data. Can be a formula
(e.g., \code{~ year}) or a character string (e.g., \code{"year"}).}

\item{group_var}{Optional name of grouping variable for panel data.
If provided, separate random walks are estimated for each group.}

\item{shared}{Logical; if TRUE (default), temporal effect enters both
numerator and denominator linear predictors identically.}
}
\value{
A \code{ratiod_temporal} object
}
\description{
Specify an independent temporal random effect: each time point gets its own
coefficient with \code{phi[t] ~ N(0, sigma^2)}, no dependence between them.

Use it for a year or season effect that varies without a trend, and as the
unstructured comparison against which \code{temporal_rw1()}, \code{temporal_rw2()}
and \code{temporal_ar1()} add smoothness.
}
\details{
The precision matrix is \code{tau * I}: diagonal and full rank, so no constraint
is needed and no ordering of the time points is used. A model whose time
points carry no meaningful order fits the same way here as one whose do.
}
\examples{
# Create temporal IID specification
temporal_iid("year")

\donttest{
# Unstructured year effects
set.seed(129)
df <- data.frame(
  year = rep(2010:2029, each = 2),
  x = rnorm(40),
  count = rpois(40, lambda = 22),
  effort = rgamma(40, shape = 4, rate = 1)
)

fit <- tratio(
  count | effort ~ x,
  data = df,
  family = ratiod_poisson_gamma(),
  temporal = temporal_iid("year"),
  mode = "hmc",
  control = list(iter = 200, warmup = 100, chains = 1)
)
}

}
